// Keep the first visible row aligned across tab, calendar, and nested screens.
// Safe-area insets are added by each screen/header; these values are content-space.
export const MOBILE_HEADER_TOP_PADDING = 0;
export const MOBILE_HEADER_ROW_HEIGHT = 58;
// Keep content immediately below the higher shared header rather than leaving
// the old vertical gap behind when the header is repositioned.
export const MOBILE_HEADER_SCROLL_SPACE = 98;
